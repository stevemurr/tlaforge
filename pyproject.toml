[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "tlaforge"
version = "0.1.0"
description = "Build TLA+ state machine specs with Python builders."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Steve Murr" }
]
